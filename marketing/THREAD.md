# Dualyne launch thread (X)

One tweet per banner in `marketing/banners/png/`. Every tweet is under 280 characters as X counts them
(a link counts as 23, an emoji as 2). Post them as one thread, in this order.

---

## 1/ Introduction

Image: `01-introduction.png`

```
Every AI model. One prompt away.

Meet Dualyne: one place for Claude, GPT, Gemini, Llama, DeepSeek and Mistral.

Chat with them, compare their answers side by side, and ship the winner with a single API key.

Here's what you can do today 🧵
```

## 2/ Features

Image: `02-features.png`

```
Built for choosing, then shipping.

→ Free AI chat, no signup
→ Side-by-side compare with speed for each answer
→ Blind votes that power a community leaderboard
→ One OpenAI-compatible API key
→ All of it on Telegram too
```

## 3/ Chat

Image: `03-chat.png`

```
Ask anything. Free, no signup.

Pick Claude, Llama, DeepSeek or Mistral and start typing. Switch models mid-conversation with one tap.

Your chat history stays in your browser. We don't store your conversations.

dualyne.com/chat
```

## 4/ Compare

Image: `04-compare.png`

```
Which model is best for your task? Ask two at once.

Same prompt, two answers side by side, with the speed of each. Vote blind, then see who wrote what.

Every vote feeds a leaderboard ranked by real people, not benchmarks.
```

## 5/ API

Image: `05-api.png`

```
Building something? Dualyne speaks the OpenAI format.

Change the base URL, keep your SDK, and switch models by changing one string. Streaming, tools and JSON mode pass straight through.

Early access: dualyne.com/docs
```

## 6/ Telegram

Image: `06-telegram.png`

```
Prefer Telegram? Meet @dualynebot.

Ask in any language and watch the answer appear as it's written. Try /compare for a blind head-to-head, or add the bot to your group and use /ask.

t.me/dualynebot
```

## 7/ Call to action

Image: none (or `x-header-1500x500.png`)

```
Dualyne is live.

Try it free, no signup: dualyne.com

Follow along for new models, features and leaderboard updates. If this helps you pick the right AI, a repost helps others find it too.
```

---

## Single post (when there's no room for a thread)

Image: `01-introduction.png`

```
Every AI model. One prompt away.

Chat with Claude, Llama, DeepSeek and Mistral for free, compare two answers side by side, and ship the winner with one OpenAI-compatible key.

No signup: dualyne.com
```

## X profile

- **Name:** Dualyne
- **Bio:** Every AI model, one prompt away. Chat free, compare answers side by side, ship with one API key. Also on Telegram: @dualynebot
- **Header:** `x-header-1500x500.png`
- **Website:** dualyne.com

## Notes

- Only claims the product backs today: free chat with the four explorer models, no signup, compare with speed
  numbers and votes, the leaderboard, the OpenAI-compatible API (in early access), and the Telegram bot.
- GPT and Gemini are in the catalog, so tweet 1 names them, but the free chat is Claude, Llama, DeepSeek and
  Mistral; the chat tweet and the single post say exactly that.
- The numbers in the banner mockups (0.4s, 200 OK) are illustrations, so the copy doesn't quote them.
