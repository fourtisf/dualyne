# Docs launch: X copy

Image: `banners/png/07-docs.png`. Post only after `API_OPEN=true` is live.

## Main post

> The Dualyne API is live, and the docs are ready.
>
> One OpenAI-compatible endpoint. Change your base URL, keep your code.
>
> → 20 free requests a day
> → Claude, Llama, DeepSeek and Mistral
> → Streaming, tools and JSON mode
>
> Your first answer in under a minute:
> dualyne.com/docs

## Thread (optional replies under the main post)

**2/**

> Already using the OpenAI SDK? Two lines change:
>
> `base_url = "https://api.dualyne.com/v1"`
> `api_key = "dly_live_…"`
>
> Everything else — messages, streaming, tools — stays exactly the same.

**3/**

> No email, no password, no card.
>
> Connect a wallet, sign one message, create a key. Keys are stored only as hashes and shown to you once.

**4/**

> Switch models by changing one string.
>
> model="claude-swift" → model="llama"
>
> Same request, same response format, so you can compare quality and speed on your own prompts.

**5/**

> Built for production habits:
> • errors in the OpenAI format
> • remaining quota in every response header
> • usage per key in your dashboard
>
> Start free: dualyne.com/docs

## Short versions

For a quote-post or reply (under 140 characters):

> One key, many models, zero code rewrites. The Dualyne API is live with 20 free requests a day. dualyne.com/docs

For Telegram or a community announcement:

> 📚 Dualyne Docs are live
>
> Use Claude, Llama, DeepSeek and Mistral through one OpenAI-compatible API. Point your existing SDK at api.dualyne.com/v1, and you're done.
>
> Free: 20 requests a day. Get your key → dualyne.com/docs
